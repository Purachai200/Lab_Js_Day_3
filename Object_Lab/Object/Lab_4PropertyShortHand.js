const username = prompt("Username : ");
const email = prompt("Email : ");
const password = prompt("Password : ");
const user = {
    username,
    email,
    password
};
alert("User : "+user.username+" Email : "+user.email+" Password : "+user.password);